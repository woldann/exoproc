import{_ as a,o as s,c as n,a3 as o}from"./chunks/framework.oxq6d-og.js";const u=JSON.parse('{"title":"Architecture overview","description":"","frontmatter":{},"headers":[],"relativePath":"en/architecture/overview.md","filePath":"en/architecture/overview.md"}'),c={name:"en/architecture/overview.md"};function r(t,e,i,p,d,l){return s(),n("div",null,[...e[0]||(e[0]=[o(`<h1 id="architecture-overview" tabindex="-1">Architecture overview <a class="header-anchor" href="#architecture-overview" aria-label="Permalink to &quot;Architecture overview&quot;">​</a></h1><p>Except for the common helpers in <code>exoproc-utils</code>, <code>bun-xffi</code> is Exoproc&#39;s functional foundation: <strong>x</strong> means cross-process and <strong>ffi</strong> is the C ABI, type, and call layer. Other packages build Windows objects, thread redirection, and hooks on that model.</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Application / example</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>  nhook · minhook</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>accessors / nthread</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>winapi (Windows process, thread, context, memory wrappers)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span> xffi (cross-process FFI: C ABI, structs, accessors, calls and memory)</span></span>
<span class="line"><span>        │</span></span>
<span class="line"><span>Windows x64</span></span></code></pre></div><h2 id="the-accessor-chain" tabindex="-1">The accessor chain <a class="header-anchor" href="#the-accessor-chain" aria-label="Permalink to &quot;The accessor chain&quot;">​</a></h2><p><code>IMemoryAccessor</code>, defined by <code>bun-xffi</code>, is the common surface for <code>read</code>, <code>write</code>, <code>alloc</code>, <code>free</code>, <code>scan</code>, and <code>call</code>. A struct therefore does not need to know whether its data is local, in another process, or reached through NThread.</p><p><code>bun-xffi</code> is not only a wrapper around local <code>bun:ffi</code>: it makes structs, pointers, C functions, and accessor operations compose across process boundaries. <code>IndirectNThreadHostAccessor</code> places NThread beneath a middleware chain for call redirection, machine-code pooling, transfer, scanning, and ABI marshalling.</p>`,6)])])}const f=a(c,[["render",r]]);export{u as __pageData,f as default};
